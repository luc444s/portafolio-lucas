# A.SPEC PORT-0003 — Tabla de stack desde datos tipados

## WHY

El visitante necesita ver de un vistazo qué domina Lucas. La tabla del
preview aprobado es el formato; hoy no existe en la app.

## WHAT

Nueva sección "Stack" en `/`: tabla de 7 filas (área → tecnologías)
renderizada desde `StackRow[]` tipado, con header primary estilo ERP.
Verdad nueva: el stack completo es dato tipado visible.

## SCOPE

- `src/data/stack.ts` con tipo y 7 entradas (backend, db, frontend,
  arquitectura, devops, entorno, metodología).
- Componente `src/sections/Stack.tsx` con `<table>` flat.

## OUT OF SCOPE

- Barras de nivel/porcentaje (no hay métrica honesta).
- Otras secciones.

## CONTRACT

- El DOM contiene una `<table>` con 7 filas de datos + header.
- Agregar/quitar fila = editar solo el array.

## INVARIANTS

```yaml
invariants:
  - Hero y Proyectos intactos.
  - Estilos flat; temas operativos; typecheck/build en verde.
```

## VERIFICATION

- `pnpm typecheck && pnpm build` exit 0.
- curl: la tabla contiene "PostgreSQL" y "ADD/A.SPEC".

## ROLLBACK

- Revert commit.

## Change Surface

```yaml
change_surface:
  allowed:
    - src/data/stack.ts
    - src/sections/Stack.tsx
    - src/App.tsx
  prohibited:
    - vendor/**
    - src/sections/Hero.tsx
    - src/data/projects.ts
```

## Blast Radius

```yaml
blast_radius:
  direct:
    - sección stack de "/"
  indirect: []
  must_not_affect:
    - hero
    - proyectos
    - sistema de temas
```

## Composition

```yaml
composition:
  requires_aspecs:
    - PORT-0002
  must_compose_with:
    - PORT-0004
  systemic_invariants:
    - orden hero → proyectos → stack → historia
  composition_checks:
    - verificación visual final tras PORT-0004
```

## Structural Constraints

```yaml
structural_constraints:
  primary_rule: datos separados de presentación
  entrypoints_must_stay_thin: true
  review_threshold_lines: 400
  extraction_threshold_lines: 600
  preferred_new_logic_locations:
    - src/data/
```

## Traceability

- Requirement: content.md §Stack que domino
- Commit:
- Deployment:

## Definition of Done

- [ ] Objective satisfied
- [ ] Scope respected
- [ ] Contract satisfied
- [ ] Independent falsable truth exists now
- [ ] Invariants preserved
- [ ] Verification passed
- [ ] Rollback / compensation is honest
- [ ] Composition checks passed when applicable
- [ ] No unrelated changes
- [ ] Structural constraints respected
- [ ] Traceability established
